####################################################################
# github.com/jantman/workstation-bootstrap  -  Global Defaults     #
#                                                                  #
# If needed, these should be overridden in user_config.yaml        #
####################################################################

classes:
  - workstation_bootstrap::firewall_pre
  - workstation_bootstrap::firewall_post
  - firewall

# This must exist, even if it's an empty array (like below)
remove_classes: []
